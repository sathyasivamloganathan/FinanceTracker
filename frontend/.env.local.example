# Copy this file to ".env.local" in the frontend/ folder.

# CHANGE THIS if your backend runs somewhere other than localhost:4000
# (e.g. once you deploy the backend, point this at its real URL).
NEXT_PUBLIC_API_URL=http://localhost:4000
